<Window x:Class="zad1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:zad1"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="400">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center">
            <Label Content="Podaj liczby:" FontSize="16" Margin="5"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="4*" />
                </Grid.ColumnDefinitions>
                <Label Content="Liczba 1:" Grid.Column="0"  FontSize="16"/>
                <TextBox x:Name="number1" Grid.Column="1"  FontSize="16"/>
            </Grid>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="4*" />
                </Grid.ColumnDefinitions>
                <Label Content="Liczba 2:" Grid.Column="0"  FontSize="16"/>
                <TextBox x:Name="number2" Grid.Column="1"  FontSize="16" />
            </Grid>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <CheckBox x:Name="addButton" Content="Dodaj" Width="80" Margin="5"/>
                <CheckBox x:Name="subtractButton" Content="Odejmij" Width="80" Margin="5"/>
                <CheckBox x:Name="multiplyButton" Content="Pomnóż" Width="80" Margin="5"/>
                <CheckBox x:Name="divideButton" Content="Podziel" Width="80" Margin="5"/>
            </StackPanel>
            <Button x:Name="calculateButton" Content="Oblicz" Width="100" Height="30" Margin="5" HorizontalAlignment="Center" Click="calculateButton_Click"/>
        </StackPanel>
        <Label x:Name="resultLabel" Grid.Row="1" Grid.Column="0" Content="" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Grid>
</Window>
